<div class="row">
  <div class="span12">
  	<div class="media-body">
        <h2 class="media-heading"><%= @user.name %></h2>
    </div>
    <div class="media">
      <% if @user.avatar? %>
        <%= image_tag @user.avatar.url %>
      <% end %>
    </div>
  </div>
</div>